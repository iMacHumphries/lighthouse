//    float time = u_time * .2;
//    vec2 sp = gl_FragCoord.xy / size.xy;
//    vec2 p = sp * 6.0 - 20.0;
//    vec2 i = p;
//    float c = 1.0;
//    float inten = .04;
//
//    for (int n = 0; n < 4; n++)
//    {
//        float t = time * (1.0 - (3.5 / float(n+1)));
//        i = p + vec2(cos(t - i.x) + sin(t + i.y), sin(t - i.y) + cos(t + i.x));
//        c += 1.0/length(vec2(p.x / (sin(i.x+t)/inten),p.y / (cos(i.y+t)/inten)));
//    }
//
//    c /= float(5);
//    c = 1.55-sqrt(c);
//    vec3 colour = vec3(pow(abs(c), 15.0));
//    vec4 color = vec4(clamp(colour + vec3(0.0, 0.17, 0.3), 0.0, .6), 0.4);
//
//    vec4 val = texture2D(texture, v_tex_coord);
//    gl_FragColor = mix(val, color, 0.25);
//    //gl_FragColor = val;